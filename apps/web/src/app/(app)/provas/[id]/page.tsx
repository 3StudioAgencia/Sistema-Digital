import { Placeholder } from "@/components/shell/Placeholder";

/**
 * Detalhe da prova — destino do botão "Ver" da listagem (DP-6). Placeholder
 * dentro do shell até o W2-C08 entregar a tela real (a navegação já funciona).
 */
export default function ProvaDetalhePage() {
  return <Placeholder titulo="Detalhe da prova" componente="C08" />;
}
