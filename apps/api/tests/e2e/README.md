# tests/e2e/ — testes ponta a ponta (reservado)

Pasta prevista no layout do monorepo (`CLAUDE.md §5.1`: `tests/{unit,integration,e2e}/`).

Os testes **E2E (Playwright ≥ 1.40)** só se aplicam à UI e nascem a partir da
**Wave 2+** (telas de domínio). Até lá esta pasta fica reservada — sem testes.

> A colocação definitiva dos specs Playwright (aqui vs. junto de `apps/web`)
> será decidida quando o primeiro fluxo E2E for implementado.
